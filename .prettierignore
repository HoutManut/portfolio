# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Written by `npm run content` — formatting them only creates churn
/src/lib/generated/

# Regenerated by the Paraglide Vite plugin on every build
/src/lib/paraglide/

# inlang's own build metadata and network-fetched plugin module cache
/project.inlang/cache/
/project.inlang/.meta.json
/project.inlang/README.md

# Vendored Canvas UI source — kept byte-identical to upstream apart from the
# patches marked LOCAL PATCH, so a re-download diffs cleanly
/src/lib/components/canvasui/
